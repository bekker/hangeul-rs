extern crate hangeul;

fn main() {
    //let cho = 'ㄱ';
    //let jung = 'ㅏ';
    //let jong = Some('ㅄ');
    //let composed = hangeul::compose(cho, jung, jong).unwrap();
    //assert_eq!(composed, '값');
    //let (cho2, jung2, jong2) = hangeul::decompose(composed).unwrap();
    //assert_eq!(cho, cho2);
    //assert_eq!(jung, jung2);
    //assert_eq!(jong, jong2);
    //println!("{}", composed);
}
